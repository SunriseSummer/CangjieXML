# 代码规范

本文件汇总本仓库历次评审中沉淀下来的代码规范要求，便于后续开发复用。每行一条规则，按主题归类。

## 注释

- 单行注释统一使用 `//`，不使用 `/** ... */` 或 `/* ... */`
- 多行块注释统一以 `/**` 开头，以 `*/` 结束，块内每行以 ` * ` 起始
- 中文注释末尾不加句号 `。`，文中也尽量避免使用句号、分号等影响视觉美学的符号
- 注释只描述"为什么/设计动机/非平凡约束"等内容，不复述代码字面含义，不记录修改过程
- 注释中引用代码元素使用反引号包裹（如 `XmlElement`）

## 命名

- 类型名用大驼峰，函数名和变量名等使用小驼峰，但字面含义都应准确清晰反映其实体功能
- 命名避免 `n/i/j/k/sb/ojbk` 这类单字母或非惯例/简单缩写，尽量用完整单词/组合表达其语义
- 常量名全大写，如果由多个单词组成，使用下划线分隔，并按语义归类（如 `UTF8_*`、`BOM_*` 等前缀）
- 用途明确时优先使用 `length/size/index/position/current` 等约定名
- 索引类变量统一用 `xxxIndex`（如 `byteIndex`，`lastWriteIndex`）

## 魔鬼数字

- 字面量/字符/字节范围出现在条件判断或边界检查时，必须替换为具名常量
- 使用具名常量后，配套写一行 `//` 注明该常量对应的字符或语义

## 排版与格式

- 缩进宽度 4 空格，行长上限 120
- lambda 形参 `param =>` 不单独占一行，应紧跟左花括号 `{`，这样 lambda 函数体可以减少一层缩进，整体更紧凑
- `case ... =>` 后如果承接的是多行表达式（如 `match` / 多行 `if` / 需要换行的调用链），表达式必须另起一行
- 链式调用层级 ≥ 5 或超长时方可拆行，否则保持单行
- 多个独立赋值或表达式不用 `;` 串在同一行

## 未使用参数与告警

- 默认实现 / 测试 mock 中未使用的参数命名为 `_`，避免 `unused` 告警噪音
- 不通过 `-Woff unused` 关闭告警，优先用 `_` 或删除未用参数
- 不为消除告警引入伪用法（如 `let _ = x`）

## 文档与实现一致性

- 文档中的示例代码要和 API 实现保持同步
- 应该为文档中的示例代码编写独立的单元测试，有变更时先改单测跑通，再提取到文档中

## 测试

- 修改项目代码后，必须跑通全部单元测试与端到端测试

## 工具链

- 提交前通过 `cjpm build` 验证可编译，遗留的 `unused` 类告警不视为错误但应尽量清零
- 与 `cjfmt` 的默认排版规则发生冲突时，以本文件中明确列出的规则为准（例如 lambda `param =>` 同行写法）
