[package]
  cjc-version = "1.0.5"
  name = "cangjie_probe"
  version = "0.1.0"
  output-type = "executable"
  compile-option = "-O2"
  description = "E2E probe — parse fixtures with tinyxml2 and emit a fingerprint JSON for diff."
  src-dir = "src"
  target-dir = "target"

[dependencies]
  tinyxml2 = { path = "../../.." }

[test-dependencies]
