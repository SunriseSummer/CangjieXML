[package]
  cjc-version = "1.0.5"
  name = "cangjie_bench"
  version = "0.1.0"
  output-type = "executable"
  compile-option = "-O2"
  description = "Perf probe — bench tinyxml2 (Cangjie) against C++ tinyxml2 / python on shared fixtures."
  src-dir = "src"
  target-dir = "target"

[dependencies]
  tinyxml2 = { path = "../.." }

[test-dependencies]
