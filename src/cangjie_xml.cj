/*
 * cangjie_xml — 顶层门面包。
 *
 * 当前仅作为 `cjpm` 构建的入口；后续 Phase 12 将在本文件（及 `facade` 子包）
 * 汇总 `public import` 所有面向下游的 API。
 */
package cangjie_xml

/** 库版本字符串；与 `cjpm.toml` 中 `version` 同步。 */
public const CANGJIE_XML_VERSION: String = "0.1.0"
